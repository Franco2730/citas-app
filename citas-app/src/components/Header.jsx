const Header = () => {
    return( //Todos los componentes necesitan un return que devuelvan un solo elemento en su mayor nivel (algo que envuelva a todos los elementos)
        <>
            <h1>Desde el header</h1>
        </>
    )
}
export default Header;
